L = [1,3,2,4,5]
# L = [1,3,6,7,9]
L = [8,6,4,4,1]

a = all(x<=y for x, y in zip(L, L[1:]))

for x, y in zip(L, L[1:]):

    store one direction
    store other direction

    if x > y:
        print(f"{x}, {y} {True}")
    else:
        print(f"{x}, {y} {False}")
        get number of false
        if 1 or less store
        add to store whie check

    if the reverse

    check both directions, if one true then safe


print(a)